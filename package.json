{
  "name": "be-nc-news",
  "version": "1.0.0",
  "description": "bc-nc-news",
  "main": "index.js",
  "scripts": {
    "setup-dbs": "psql -f ./db/setup.sql",
    "seed": "knex seed:run",
    "migrate-latest": "knex migrate:latest",
    "migrate-rollback": "knex migrate:rollback",
    "seed-from-scratch": "npm run setup-dbs && npm run migrate-latest && npm run seed && NODE_ENV=test npm run migrate-latest && NODE_ENV=test npm run seed",
    "test": "jest --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/northcoders/be-nc-news.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/northcoders/be-nc-news/issues"
  },
  "homepage": "https://github.com/northcoders/be-nc-news#readme",
  "dependencies": {
    "knex": "^0.21.2",
    "pg": "^8.3.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.8",
    "@types/knex": "^0.16.1",
    "jest": "^26.2.2"
  }
}
